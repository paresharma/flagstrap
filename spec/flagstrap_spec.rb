require "spec_helper"

RSpec.describe Flagstrap do
  it "has a version number" do
    expect(Flagstrap::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
